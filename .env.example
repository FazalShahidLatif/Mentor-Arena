# MongoDB Atlas (free tier) — persistent database for leads, config, and sessions
# Create a free cluster at https://cloud.mongodb.com → Build a Database → Free → M0
# Get your connection string from Database Deployments → Connect → Drivers → Node.js
# Replace <password> with your database user password
MONGODB_URI="mongodb+srv://mentor:***@cluster0.5ph77.mongodb.net/mentor?retryWrites=true&w=majority"

# Admin Dashboard Password (default: admin123)
ADMIN_PASSWORD=admin123
